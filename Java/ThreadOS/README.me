Multi-threaded application which simulates an operating system. Allows for running of dev applications, in this case "tests" and allows creation and persistence of files through a filesytem implementation utilizing a DISK image.
